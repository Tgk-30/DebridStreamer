# YAWF Stream v0.9.18 Verification Log

Date: 2026-07-18

Release tag: `v0.9.18-web`

Website: `https://tgk30.com/debridstreamer/`

This log records the behavior and release gates verified for v0.9.18. It does not contain credentials, private prompts, or API responses.

## Kimi K3 design review

- Called the official Kimi coding API at `https://api.kimi.com/coding/v1/chat/completions` with model `k3`.
- Received HTTP 200 and a completed UI, UX, and implementation review.
- Applied the recommendations that fit the existing YAWF Stream design system: honest cache states, isolated source diagnostics, bounded provider checks, focus-safe overlays, keyboard calendar navigation, simpler download messaging, and fewer simulated status elements.
- Kept the real Assistant feature. Removed only decorative or simulated AI and cache theater that did not perform a useful action.

## Cache and indexing

- Verified IMDb and title searches use independent diagnostic state while sharing configured indexers.
- Verified failure in one search pass does not erase healthy results from the other pass.
- Verified an outage is shown only when all applicable source paths fail.
- Verified duplicate source errors are merged without losing the affected source name.
- Verified cache hashes are matched case-insensitively.
- Verified a confirmed provider response maps to `cached` or `not_cached`.
- Verified missing, timed out, or failed provider responses map to `unavailable`, not the misleading `will cache` state.
- Verified each debrid cache provider is bounded by a 10 second timeout.
- Verified a hung provider cannot block a healthy provider's cached result.
- Verified abort signals are accepted by the cache aggregation path.

## Application UI and accessibility

- Verified the main navigation becomes inert while Browse or Detail overlays are open.
- Verified reduced-motion mode does not mount the decorative ambient video.
- Verified the hero image and controls remain visible when the desktop window is suspended, unfocused, or input-idle.
- Verified search controls, calendar releases, and removable library items have visible keyboard focus.
- Verified removable library controls remain reachable on touch and coarse pointer devices.
- Verified reset and erase dialogs fit inside the viewport and keep their full confirmation content and actions visible.
- Verified the first-run copy accurately distinguishes the minimal device setup from the advanced setup path.
- Verified Settings uses the neutral Midnight Studio default, live appearance previews, clearer disclosure sections, six accent choices, and readable option spacing.
- Verified Profiles offers 80 avatar choices across five categories, 16 profile colors, custom color support, and custom photo support.
- Verified user-facing copy added in this release contains no em dash characters.

## Calendar

- Verified the calendar defaults to agenda view below 768 px and month view on wider screens.
- Verified calendar view and release-type filters persist locally.
- Verified the month grid uses one roving keyboard tab stop.
- Verified Left, Right, Up, Down, Home, End, Page Up, Page Down, Shift plus Page Up or Page Down, and `T` navigation.
- Verified today's cell exposes `aria-current="date"`.
- Verified day labels announce the date, today state, and release count.
- Verified keyboard focus is restored after month navigation.
- Verified release cards have visible focus rings.

## Website and download flow

- Verified all product copy uses YAWF Stream and `yawf.com/stream` where the product URL is shown.
- Verified the footer includes `Copyright 2026 YAWF Group`.
- Verified download rows point directly to the v0.9.18 macOS Apple Silicon, macOS Intel, Windows MSI, Linux AppImage, and server Debian assets.
- Verified the server install command uses `debridstreamer-server_0.9.18_all.deb`.
- Verified simulated cache scans, fake live indicators, fake source counts, and the decorative instant-only sorter are absent from the download page.
- Verified the platform picker wraps cleanly on a 390 px phone viewport.
- Verified the mobile navigation is a dialog, traps focus, closes with Escape, makes background regions inert, and returns focus to the menu trigger.
- Verified the theme menu exposes radio selection semantics and returns focus to its trigger.
- Verified route-level lazy loading keeps the 3D bundle off the initial route chunk.
- Verified the development source-inspection plugin is absent from production builds.

## Visual checks

- Desktop website home: hero, brand ring, primary call to action, navigation, footer, and copyright checked.
- Desktop website download: direct platform rows, release version, labels, and spacing checked.
- Mobile website download at 390 by 844: header wrapping, rows, navigation dialog, focus return, and footer checked.
- Desktop app Settings: appearance cards, controls, section hierarchy, contrast, spacing, and profile controls checked.
- Desktop app reset dialog: centering, viewport fit, dimming, content, and actions checked without performing a destructive action.
- Desktop app Calendar: setup empty state and layout checked. Populated month, agenda, and keyboard behavior are also covered by automated tests.
- Mobile app home at 390 by 844: hero artwork, title, Play, More info, and bottom navigation checked after the suspended-animation fix.

## Automated verification

- Web app: 213 test files and 3,457 tests passed.
- Web typecheck: passed.
- Web production build: passed.
- Web coverage: 86.20% statements, 80.98% branches, 82.61% functions, and 88.57% lines.
- Server: 9 test files and 118 tests passed.
- Server typecheck and production build: passed.
- Swift: 99 suites and 532 tests passed.
- Rust: 29 tests passed.
- Website ESLint: passed.
- Website production build: passed.
- App responsive contract: passed.
- Website app, download logic, static, and mounted-path checks: passed.
- Release readiness checks: passed.
- Public repository preflight for tracked, untracked, and reachable HEAD content: passed.
- Git diff whitespace check: passed.
- Version consistency check: no stale `0.9.17` references found in source.

## Known non-blocking warnings

- The web build reports the existing HLS chunk above 500 kB after minification.
- The website build reports the initial shared route chunk and async React Three Fiber chunk above 500 kB after minification. The 3D chunk is loaded asynchronously.
- Browserslist reports that its local `caniuse-lite` data is seven months old.
- Rust reports 43 existing OpenGL and unsafe-code warnings while all Rust tests pass.
