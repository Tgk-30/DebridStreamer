# YAWF Stream v0.9.21 Verification Log

Date: 2026-07-19

Release tag: `v0.9.21-web`

Website: `https://tgk30.com/debridstreamer/`

This log records the server connection hotfix and local release verification
for v0.9.21. It does not contain credentials, private service responses,
session values, or raw provider URLs.

## Server connection hotfix

- Replaced the onboarding cross-origin health probe with direct navigation to
  the supplied server address.
- Bare hostnames are normalized to HTTPS before navigation.
- HTTP and HTTPS addresses remain supported.
- Invite paths, query parameters, and fragments are preserved.
- Invalid schemes and navigation failures keep the user on the connection
  screen with an actionable error.
- Added focused coverage for empty input, bare hosts, invite links, invalid
  schemes, successful navigation, and navigation failure.

## Release metadata

- Synchronized the web app, bundled server, Tauri package, Rust crate, website,
  and lockfiles at version 0.9.21.
- Updated the public website download metadata to `v0.9.21-web`.
- Kept the Windows installer signing status unchanged under the accepted v0.9
  beta risk recorded in `SEC-008`.

## Automated verification

- Focused onboarding suite: 36 tests passed.
- Full web app suite: 217 test files and 3,471 tests passed.
- Web typecheck and production build: passed.
- Server suite: 9 test files and 124 tests passed.
- Server typecheck and production build: passed.
- Provider smoke contracts: 4 tests passed.
- Swift: 99 suites and 532 tests passed.
- Rust: 29 tests passed, and `cargo check --locked` passed.
- Website ESLint and production build: passed.
- Release readiness, security decisions, public repository target preflight,
  website QA, mounted-path QA, download logic, static-site QA, and responsive
  contracts: passed.
- Release workflow syntax checks passed with actionlint.

## Publication gate

- The release workflow must build the macOS, Windows, and Linux packages.
- The clean-install matrix must pass against the draft assets before the draft
  release is published.
- The updater manifest and public website download links must resolve to the
  published v0.9.21 assets.

## Known non-blocking warnings

- Rust reports existing OpenGL deprecation and unnecessary-unsafe warnings.
- Web and website builds report existing chunks above 500 kB after
  minification.
- Browserslist reports that local browser compatibility data is seven months
  old.
