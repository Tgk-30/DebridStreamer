// Placeholder shell. Phase 1 groundwork is the service layer (see
// src/services/metadata). UI is intentionally out of scope for this phase.
export function App() {
  return <h1>DebridStreamer (web) — service layer scaffold</h1>;
}
